import { flagExtension, flagTypies } from "./index";

// to be fixed later
// remove any type and assign it to flagType value
export interface IModuleConfig {
	flagExtensions: flagExtension;
	flagType: flagTypies;
	flagSize: any;
}
